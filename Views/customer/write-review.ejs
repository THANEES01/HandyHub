<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write a Review - <%= booking.provider_name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        :root {
            --primary-blue: #2E86AB;
            --dark-blue: #1E5A75;
            --light-blue: #E8F4F8;
            --gold: #FFD700;
            --gold-hover: #FFC107;
            --gray-light: #F8F9FA;
            --gray-medium: #E9ECEF;
            --gray-dark: #6C757D;
        }
        
        body {
            background-color: var(--gray-light);
        }
        
        .review-container {
            max-width: 750px;
            margin: 0 auto;
        }
        
        .review-header {
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--dark-blue) 100%);
            color: white;
            padding: 40px;
            border-radius: 20px 20px 0 0;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .review-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" fill="rgba(255,255,255,0.1)"><polygon points="0,20 50,0 100,20 200,0 300,20 400,0 500,20 600,0 700,20 800,0 900,20 1000,0 1000,100 0,100"/></svg>') repeat-x;
            background-size: 100px 100px;
        }
        
        .review-header h2 {
            position: relative;
            z-index: 1;
            font-weight: 700;
            margin-bottom: 10px;
        }
        
        .review-header p {
            position: relative;
            z-index: 1;
            opacity: 0.9;
        }
        
        .review-content {
            background-color: white;
            padding: 40px;
            border-radius: 0 0 20px 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
        }
        
        .booking-info {
            background: linear-gradient(135deg, var(--light-blue) 0%, #F0F8FF 100%);
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 40px;
            border: 1px solid rgba(46, 134, 171, 0.1);
            position: relative;
        }
        
        .booking-info::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-blue), var(--gold));
            border-radius: 15px 15px 0 0;
        }
        
        .booking-info h4 {
            color: var(--dark-blue);
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .service-details p {
            margin-bottom: 8px;
            color: #495057;
        }
        
        .service-details strong {
            color: var(--dark-blue);
            font-weight: 600;
        }
        
        /* Enhanced Star Rating */
        .rating-section {
            background: #FAFAFA;
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            border: 2px solid var(--gray-medium);
            transition: all 0.3s ease;
        }
        
        .rating-section:focus-within {
            border-color: var(--primary-blue);
            box-shadow: 0 0 20px rgba(46, 134, 171, 0.1);
        }
        
        .rating-label {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--dark-blue);
            margin-bottom: 15px;
            text-align: center;
        }
        
        .rating-guide {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            padding: 0 15px;
        }
        
        .rating-guide span {
            font-size: 0.85rem;
            color: var(--gray-dark);
            font-weight: 500;
        }
        
        .star-rating {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
        }
        
        .star-rating input {
            display: none;
        }
        
        .star-rating label {
            font-size: 3rem;
            color: var(--gray-medium);
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            padding: 5px;
        }
        
        .star-rating label:hover {
            transform: scale(1.2);
            filter: drop-shadow(0 0 10px rgba(255, 215, 0, 0.5));
        }
        
        .star-rating input:checked ~ label,
        .star-rating label:hover,
        .star-rating label:hover ~ label {
            color: var(--gold);
            animation: pulse 0.3s ease;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        .rating-feedback {
            text-align: center;
            margin-top: 15px;
            padding: 10px;
            border-radius: 10px;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--dark-blue) 100%);
            color: white;
            font-weight: 600;
            opacity: 0;
            transform: translateY(10px);
            transition: all 0.3s ease;
        }
        
        .rating-feedback.show {
            opacity: 1;
            transform: translateY(0);
        }
        
        /* Form Elements */
        .form-label {
            font-weight: 600;
            color: var(--dark-blue);
            margin-bottom: 10px;
        }
        
        .form-control, .form-select {
            border: 2px solid var(--gray-medium);
            border-radius: 10px;
            padding: 12px 16px;
            transition: all 0.3s ease;
            font-size: 0.95rem;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: var(--primary-blue);
            box-shadow: 0 0 0 0.2rem rgba(46, 134, 171, 0.25);
        }
        
        .form-check {
            background: var(--gray-light);
            padding: 15px;
            border-radius: 10px;
            border: 1px solid var(--gray-medium);
            transition: all 0.3s ease;
        }
        
        .form-check:hover {
            background: #FDFDFD;
        }
        
        .form-check-input:checked {
            background-color: var(--primary-blue);
            border-color: var(--primary-blue);
        }
        
        .form-check-label {
            color: var(--dark-blue);
            font-weight: 500;
        }
        
        /* Buttons */
        .btn-submit-review {
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--dark-blue) 100%);
            color: white;
            padding: 15px 35px;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1.05rem;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(46, 134, 171, 0.3);
        }
        
        .btn-submit-review:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(46, 134, 171, 0.4);
            color: white;
        }
        
        .btn-outline-secondary {
            border: 2px solid var(--gray-medium);
            color: var(--gray-dark);
            padding: 15px 35px;
            border-radius: 12px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .btn-outline-secondary:hover {
            border-color: var(--primary-blue);
            color: var(--primary-blue);
            background: rgba(46, 134, 171, 0.05);
        }
        
        .back-link {
            color: var(--primary-blue);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            margin-bottom: 30px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .back-link:hover {
            text-decoration: none;
            color: var(--dark-blue);
            transform: translateX(-5px);
        }
        
        .back-link i {
            margin-right: 8px;
            transition: all 0.3s ease;
        }
        
        /* Review Tips */
        .review-tip {
            background: linear-gradient(135deg, #F8F9FA 0%, #E9ECEF 100%);
            padding: 25px;
            border-radius: 15px;
            margin-top: 30px;
            border-left: 5px solid var(--primary-blue);
            box-shadow: 0 3px 10px rgba(0,0,0,0.05);
        }
        
        .review-tip h6 {
            color: var(--dark-blue);
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .review-tip ul {
            margin-bottom: 0;
            padding-left: 25px;
        }
        
        .review-tip li {
            color: #495057;
            font-size: 0.9rem;
            margin-bottom: 8px;
            line-height: 1.6;
        }
        
        /* Character Counter */
        .char-counter {
            text-align: right;
            font-size: 0.85rem;
            margin-top: 5px;
            transition: all 0.3s ease;
        }
        
        .char-counter.success {
            color: #28a745;
        }
        
        .char-counter.warning {
            color: #ffc107;
        }
        
        .char-counter.error {
            color: #dc3545;
        }
        
        /* Animations */
        .fade-in {
            animation: fadeIn 0.5s ease;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .review-header {
                padding: 30px 20px;
            }
            
            .review-content {
                padding: 30px 20px;
            }
            
            .star-rating label {
                font-size: 2.5rem;
            }
            
            .rating-guide {
                padding: 0 5px;
            }
            
            .rating-guide span {
                font-size: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <%- include('../partials/navbar') %>

    <div class="container mt-5 mb-5">
        <a href="/customer/bookings" class="back-link">
            <i class="bi bi-arrow-left"></i> Back to My Bookings
        </a>
        
        <div class="review-container fade-in">
            <% if (locals.error) { %>
                <div class="alert alert-danger" role="alert">
                    <i class="bi bi-exclamation-triangle-fill"></i>
                    <%= error %>
                </div>
            <% } %>
            
            <div class="review-header">
                <h2><i class="bi bi-star-fill me-2"></i>Write a Review</h2>
                <p class="mb-0">Share your experience with this service provider</p>
            </div>
            
            <div class="review-content">
                <div class="booking-info">
                    <h4><i class="bi bi-person-gear me-2"></i><%= booking.provider_name %></h4>
                    <div class="service-details">
                        <p><strong>Service Type:</strong> <%= booking.service_type %></p>
                        <p><strong>Service Date:</strong> 
                            <%= new Date(booking.preferred_date).toLocaleDateString('en-US', {
                                weekday: 'long',
                                year: 'numeric',
                                month: 'long',
                                day: 'numeric'
                            }) %>
                        </p>
                        <p class="mb-0"><strong>Booking ID:</strong> #<%= booking.id %></p>
                    </div>
                </div>
                
                <form action="/customer/submit-review" method="POST" id="reviewForm">
                    <input type="hidden" name="booking_id" value="<%= booking.id %>">
                    <input type="hidden" name="provider_id" value="<%= booking.provider_id %>">
                    
                    <div class="rating-section">
                        <div class="rating-label">How would you rate this service?</div>
                        <div class="rating-guide">
                            <span>Poor</span>
                            <span>Fair</span>
                            <span>Good</span>
                            <span>Very Good</span>
                            <span>Excellent</span>
                        </div>
                        <div class="star-rating">
                            <input type="radio" id="star5" name="rating" value="5" required>
                            <label for="star5" title="5 stars - Excellent" data-rating="Excellent"><i class="bi bi-star-fill"></i></label>
                            
                            <input type="radio" id="star4" name="rating" value="4">
                            <label for="star4" title="4 stars - Very Good" data-rating="Very Good"><i class="bi bi-star-fill"></i></label>
                            
                            <input type="radio" id="star3" name="rating" value="3">
                            <label for="star3" title="3 stars - Good" data-rating="Good"><i class="bi bi-star-fill"></i></label>
                            
                            <input type="radio" id="star2" name="rating" value="2">
                            <label for="star2" title="2 stars - Fair" data-rating="Fair"><i class="bi bi-star-fill"></i></label>
                            
                            <input type="radio" id="star1" name="rating" value="1">
                            <label for="star1" title="1 star - Poor" data-rating="Poor"><i class="bi bi-star-fill"></i></label>
                        </div>
                        <div class="rating-feedback" id="ratingFeedback"></div>
                    </div>
                    
                    <div class="mb-4">
                        <label for="reviewText" class="form-label">Share your experience</label>
                        <textarea class="form-control" id="reviewText" name="review_text" rows="5" 
                                  placeholder="Describe your experience with this service provider. How was the quality of work? Was the provider professional and on time? Would you recommend them to others?"></textarea>
                        <div class="char-counter" id="charCounter">0 characters</div>
                    </div>
                    
                    <div class="form-check mb-4">
                        <input class="form-check-input" type="checkbox" id="recommend" name="recommend" value="1">
                        <label class="form-check-label" for="recommend">
                            <i class="bi bi-hand-thumbs-up me-2"></i>
                            I would recommend this service provider to others
                        </label>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/customer/bookings" class="btn btn-outline-secondary me-md-2">
                            <i class="bi bi-x-lg me-1"></i>Cancel
                        </a>
                        <button type="submit" class="btn btn-submit-review">
                            <i class="bi bi-send me-1"></i>Submit Review
                        </button>
                    </div>
                </form>
                
                <div class="review-tip">
                    <h6><i class="bi bi-lightbulb-fill me-2"></i>Tips for writing a helpful review:</h6>
                    <ul>
                        <li>Be specific about what aspects of the service you liked or disliked</li>
                        <li>Mention if the provider was punctual and professional</li>
                        <li>Comment on the quality of work performed</li>
                        <li>Share if the provider communicated well throughout the process</li>
                        <li>Be honest and fair in your assessment</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Enhanced star rating functionality
            const stars = document.querySelectorAll('.star-rating label');
            const starInputs = document.querySelectorAll('.star-rating input');
            const ratingFeedback = document.getElementById('ratingFeedback');
            
            // Function to highlight stars up to a certain rating
            function highlightStars(rating) {
                stars.forEach((star, index) => {
                    const starValue = star.getAttribute('for').replace('star', '');
                    const starIcon = star.querySelector('i');
                    
                    if (starValue <= rating) {
                        starIcon.style.color = 'var(--gold)';
                        star.style.transform = 'scale(1.1)';
                    } else {
                        starIcon.style.color = 'var(--gray-medium)';
                        star.style.transform = 'scale(1)';
                    }
                });
            }
            
            // Add event listeners for hover and click effects
            stars.forEach((star, index) => {
                star.addEventListener('mouseenter', function() {
                    const rating = this.getAttribute('for').replace('star', '');
                    highlightStars(rating);
                    
                    // Show rating text
                    const ratingText = this.getAttribute('data-rating');
                    ratingFeedback.textContent = `${ratingText}`;
                    ratingFeedback.classList.add('show');
                });
                
                star.addEventListener('click', function() {
                    const rating = this.getAttribute('for').replace('star', '');
                    const ratingText = this.getAttribute('data-rating');
                    
                    highlightStars(rating);
                    ratingFeedback.textContent = `You selected: ${ratingText}`;
                    ratingFeedback.classList.add('show');
                    
                    // Add selected effect
                    stars.forEach(s => s.classList.remove('selected'));
                    this.classList.add('selected');
                });
            });
            
            // Reset stars when mouse leaves the rating area
            document.querySelector('.star-rating').addEventListener('mouseleave', function() {
                const checkedInput = document.querySelector('.star-rating input:checked');
                if (checkedInput) {
                    const rating = checkedInput.value;
                    const selectedStar = document.querySelector(`label[for="star${rating}"]`);
                    const ratingText = selectedStar.getAttribute('data-rating');
                    
                    highlightStars(rating);
                    ratingFeedback.textContent = `You selected: ${ratingText}`;
                    ratingFeedback.classList.add('show');
                } else {
                    highlightStars(0);
                    ratingFeedback.classList.remove('show');
                }
            });
            
            // Character counter for review text
            const reviewTextArea = document.getElementById('reviewText');
            const charCounter = document.getElementById('charCounter');
            
            function updateCharCount() {
                const current = reviewTextArea.value.length;
                charCounter.textContent = `${current} characters`;
                
                if (current < 10) {
                    charCounter.className = 'char-counter warning';
                } else if (current > 500) {
                    charCounter.className = 'char-counter error';
                    charCounter.textContent += ' (too long - please keep it concise)';
                } else {
                    charCounter.className = 'char-counter success';
                }
            }
            
            reviewTextArea.addEventListener('input', updateCharCount);
            updateCharCount(); // Initial call
            
            // Form validation with enhanced UX
            const reviewForm = document.getElementById('reviewForm');
            reviewForm.addEventListener('submit', function(e) {
                const rating = document.querySelector('input[name="rating"]:checked');
                const reviewText = reviewTextArea.value.trim();
                
                if (!rating) {
                    e.preventDefault();
                    // Animate the rating section to draw attention
                    const ratingSection = document.querySelector('.rating-section');
                    ratingSection.style.borderColor = '#dc3545';
                    ratingSection.style.animation = 'shake 0.5s ease';
                    
                    setTimeout(() => {
                        ratingSection.style.borderColor = '';
                        ratingSection.style.animation = '';
                    }, 500);
                    
                    alert('Please select a rating before submitting your review.');
                    return;
                }
                
                if (reviewText.length < 10) {
                    e.preventDefault();
                    reviewTextArea.style.borderColor = '#dc3545';
                    reviewTextArea.focus();
                    
                    setTimeout(() => {
                        reviewTextArea.style.borderColor = '';
                    }, 2000);
                    
                    alert('Please write at least 10 characters for your review.');
                    return;
                }
                
                // Show loading state
                const submitBtn = document.querySelector('.btn-submit-review');
                submitBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Submitting...';
                submitBtn.disabled = true;
            });
        });
        
        // Add shake animation for error feedback
        const style = document.createElement('style');
        style.textContent = `
            @keyframes shake {
                0%, 100% { transform: translateX(0); }
                25% { transform: translateX(-5px); }
                75% { transform: translateX(5px); }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>